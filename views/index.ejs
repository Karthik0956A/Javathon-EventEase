<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <title>EventEase</title>
    <style>
        /* Shared Styles */
        body {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, Helvetica, sans-serif;
            background-color: #eeeeea;
        }
        .navbar {
            background-color: #eeeeea;
            color: #fff;
            border-radius: 15px;
            padding-left: 20px;
        }
        .navbar-brand {
            font-weight: bold;
            color: #fff;
            cursor: pointer;
        }
        .nav-link {
            color: #fff;
        }
        .nav-link:hover {
            color: #ccc;
        }
        .logout {
            margin-left: auto;
            margin-right: 2rem;
            color: #000;
        }
        .offcanvas {
            background-color: #222;
            color: white;
        }

        /* Welcome Page Styles */
        .welcome-container {
            max-width: 600px;
            margin: 50px auto;
            text-align: center;
            color: #000;
        }
        .welcome-container .headline {
            font-size: 86px;
            font-weight: 1000;
            line-height: 1.1;
            margin: 0;
            
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.6);
        }
        .welcome-container .subtext {
            font-size: 18px;
            margin-top: 20px;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.6);
        }
        .button-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 40px;
        }
        .button {
            background-color: transparent;
            color: #fff;
            border: 2px solid #fff;
            border-radius: 30px;
            padding: 10px 20px;
            font-size: 20px;
            font-weight: 700;
            text-transform: uppercase;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.6);
        }
        .button:hover {
            background-color: #fff;
            color: #000;
        }
        .button svg {
            width: 16px;
            height: 16px;
        }

        /* card styles */
        .card {
            width: 100%;
            height: 300px;
            display: flex;
            flex-direction: column;
        }

        .card img {
            width: 100%;
            height: 50%;
            object-fit: cover; /* Ensures the image fits nicely without distortion */
            border-top-left-radius: 0.375rem; /* Matches Bootstrap card border radius */
            border-top-right-radius: 0.375rem;
        }

        .card-body {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .container {
            padding: 20px;
        }

        .card-title {
            font-size: 1.1rem;
            font-weight: bold;
            color: #333;
        }

        .card-text {
            font-size: 0.9rem;
            color: #555;
        }

        @font-face {
            font-family: 'Gortez';
            src: url('https://fonts.cdnfonts.com/s/26364/Gortez-Regular.woff') format('woff');
        }

        .carousel-indicators button {
            width: max-content!important;
        }
        .carousel-indicators {
            position: unset;
            margin-top: 20px;
        }
        .info-card {
            background-color: #004D43;
            color: white;
            border-radius: 15px;
            padding: 40px;
            text-align: left;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            width: 70%;
            height: 60%;
            font-family: 'Gortez', sans-serif;
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
        }

        .info-card h5 {
            font-size: 70px;
            font-weight: bold;
            margin-bottom: 20px;
            text-shadow: 3px 3px 5px rgba(0, 0, 0, 0.6);
            line-height: 1.1;
            margin-top: 0;
            text-align: center;
        }

        .info-card h4 {
            font-size: 28px;
            text-align: center;
            margin-top: 10px;
        }

        .info-card p {
            font-size: 16px;
            text-align: center;
            margin-top: auto;
            font-style: italic;
        }

        /* Dashboard Styles */
        .carousel-inner {
            height: 80vh;
        }
        .info-card {
            background-color: #004D43;
            color: white;
            border-radius: 15px;
            padding: 40px;
            text-align: center;
            font-family: 'Arial', sans-serif;
        }
        .create-btn {
            font-size: 24px;
            font-weight: bold;
            padding: 15px 30px;
            color: #004D43;
            background-color: #FFFFFF;
            border: 2px solid #004D43;
            border-radius: 50px;
            text-transform: uppercase;
            transition: all 0.3s ease-in-out;
        }
        .create-btn:hover {
            background-color: #004D43;
            color: white;
            transform: scale(1.1);
        }
        
        body.light-mode {
            background-color: #ffffff;
            color: #000000;
        }

        body.dark-mode {
            background-color: #121212;
            color: #ffffff;
        }

       

        .card-light-mode {
            background-color: #ffffff;
            color: #000000;
        }

        .card-dark-mode {
            background-color: #1e1e1e;
            color: #ffffff;
        }

        body {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, Helvetica, sans-serif;
            background-color: #eeeeea;
        }

        .card {
            width: 100%;
            height: 300px;
            display: flex;
            flex-direction: column;
        }

        .card img {
            width: 100%;
            height: 50%;
            object-fit: cover; /* Ensures the image fits nicely without distortion */
            border-top-left-radius: 0.375rem; /* Matches Bootstrap card border radius */
            border-top-right-radius: 0.375rem;
        }

        .card-body {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .container {
            padding: 20px;
        }

        .card-title {
            font-size: 1.1rem;
            font-weight: bold;
            color: #333;
        }

        .card-text {
            font-size: 0.9rem;
            color: #555;
        }

        @font-face {
            font-family: 'Gortez';
            src: url('https://fonts.cdnfonts.com/s/26364/Gortez-Regular.woff') format('woff');
        }

        .carousel-indicators button {
            width: max-content!important;
        }
        .carousel-indicators {
            position: unset;
            margin-top: 20px;
        }
        .info-card {
            background-color: #004D43;
            color: white;
            border-radius: 15px;
            padding: 40px;
            text-align: left;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            width: 70%;
            height: 60%;
            font-family: 'Gortez', sans-serif;
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
        }

        .info-card h5 {
            font-size: 70px;
            font-weight: bold;
            margin-bottom: 20px;
            text-shadow: 3px 3px 5px rgba(0, 0, 0, 0.6);
            line-height: 1.1;
            margin-top: 0;
            text-align: center;
        }

        .info-card h4 {
            font-size: 28px;
            text-align: center;
            margin-top: 10px;
        }

        .info-card p {
            font-size: 16px;
            text-align: center;
            margin-top: auto;
            font-style: italic;
        }

        .create-btn {
            font-size: 24px;
            font-weight: bold;
            padding: 15px 30px;
            color: #004D43;
            background-color: #FFFFFF;
            border: 2px solid #004D43;
            border-radius: 50px;
            text-transform: uppercase;
            transition: all 0.3s ease-in-out;
            margin-top: auto; /* Push the button towards the lower part */
            align-self: center; /* Center the button horizontally */
        }

        .create-btn:hover {
            background-color: #004D43;
            color: white;
            transform: scale(1.1);
        }


        .create-btn {
            font-weight: bold;
            padding: 15px 30px;
            color: #004D43;
            background-color: #FFFFFF;
            border: 2px solid #004D43;
            border-radius: 50px;
            text-transform: uppercase;
            transition: all 0.3s ease-in-out;
        }

        .create-btn:hover {
            background-color: #004D43;
            color: white;
            transform: scale(1.1);
        }
    </style>
</head>
<body>
   <%-include('partials/navbar')%>

    <!-- Welcome Page -->
    <div class="welcome-container" id="welcome-section">
        <h1 class="headline">WE CREATE<br>MEMORABLE<br>EVENT EXPERIENCES</h1>
        <p class="subtext">Turning your vision into reality</p>
        <div class="button-container">
            <a href="/dashboard" class="button">
                Go to Dashboard
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <line x1="5" y1="12" x2="19" y2="12"></line>
                    <polyline points="12 5 19 12 12 19"></polyline>
                </svg>
            </a>
            <button class="button create-btn" onclick="window.location.href=`/dashboard/create`">Create Event</button>
        </div>
    </div>

    <!-- Dashboard -->
    <div id="dashboard-section" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <div class="d-flex justify-content-center align-items-center">
                    <div class="info-card">
                        <h5>WELCOME, <%= username.username %></h5>
                        <p>Let's get started with the first event</p>
                    </div>
                </div>
            </div>
            <div class="carousel-item">
                <div class="d-flex justify-content-center align-items-center">
                    <div class="info-card">
                        <h5>Plan Your Event</h5>
                        <button class="create-btn">Create Event</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <center><span style="font-size: 180%;font-weight: 700;">All Events</span></center>
    <div id="container" class="container row"></div>
    <hr>
    <center><span style="font-size: 180%;font-weight: 700;">Our Work</span></center>
    <%-include('partials/slider')%>
    <div style="height: 10vh;"></div>
    <%-include('partials/footer')%>
    <script>
         async function loadEvents() {
        try {
            const response = await fetch('/api/events');
            if (!response.ok) throw new Error('Failed to fetch events');
            const events = await response.json();

            const container = document.getElementById('container');
            container.innerHTML = ''; // Clear container

            events.forEach(event => {
                const card = document.createElement('div');
                card.className = 'col-md-4 mb-4'; // Bootstrap column
                
                card.innerHTML = `
                    <div class="card shadow-sm h-100">
                        <img src="${event.image || 'https://via.placeholder.com/150'}" alt="${event.eventName}" class="card-img-top">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">${event.eventName}</h5>
                            <p class="card-text">
                                <strong>Location:</strong> ${event.eventLocation}<br>
                                <strong>Date:</strong> ${new Date(event.eventDate).toLocaleDateString()}<br>
                                <strong>Time:</strong> ${event.eventTime}<br>
                                <strong>Description:</strong> ${event.description.substring(0, 50)}...
                            </p>
                            <div class="mt-auto"> <!-- Push button to the bottom -->
                                <a href="/events/${event._id}" class="btn btn-primary btn-sm w-100">View Details</a>
                            </div>
                        </div>
                    </div>
                `;
                container.appendChild(card);
            });
        } catch (error) {
            console.error(error);
            document.getElementById('container').innerHTML = '<p class="text-danger">Failed to load events.</p>';
        }
    }

    window.onload = loadEvents;   
        

    </script>
</body>
</html>